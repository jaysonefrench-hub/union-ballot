<%= brand.org %> <% if (brand.local) { %><%= brand.local %> · <% } %><%= brand.system %>
<% if (user && user.role === 'admin') { %> Elections Roster Accounts Audit log <% } else if (user && user.role === 'observer') { %> Observer station Audit log <% } %>
<% if (user) { %> <%= user.name %> (<%= user.role %>)
Sign out
<% } else { %> Committee sign-in <% } %>
<% if (flash) { %>
<%= flash.text %>
<% } %>
